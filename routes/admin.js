const express = require('express')
const router = express.Router()
const path = require('path')

// enter routes.use here from app.js file. This will be so the admin users can update allergy list and events

module.exports = router